import React, { useState, useEffect } from "react";

import styled from "styled-components";

const Keypad = () => {
  useEffect(() => {}, []);

  return <KeypadWrap>Keypad</KeypadWrap>;
};

const KeypadWrap = styled.div`
  width: 100%;
`;

export default Keypad;
