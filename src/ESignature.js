import React from "react";
import styled from "styled-components";
const ESignature = () => {
  return <Sign>ESignature</Sign>;
};

const Sign = styled.div`
  position: absolute;
  right: -50%;
  top: 0;
`;

export default ESignature;
